name: Deploy Velody

on:
  push:
    branches: ['master', '76-configure-for-cicd']
  pull_request:
    branches: ['master']

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: Build the docker image
        run: docker build -t velody .
